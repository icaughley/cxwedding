<%= simple_form_for(@gift, remote: true, html: {class: "form-horizontal"}) do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :location %>
  <%= f.input :value, wrapper: :prepend do %>
    <span class="add-on">$</span><%= f.input_field :value %>
  <% end %>
<% end %>
